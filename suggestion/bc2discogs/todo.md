so we want to build a tool for this existing project we will call "correlate" ... the input is a bandcamp url and then we try to find the release on discogs. This is a non-trivial task because many groups can have the same name so we'll need to use the artist and release name and be tolerant of small differences since discogs often includes (###) after an artist name to specify which one and so on ... so something like demaru-levenshtein or some other similarity score might be appropriate ... this should be a library with a if main in it so I can run it also as a tool. If you look inside this directory you'll see "api.md" which is old code but it has discogs api. The folder here discogs-api has the documentation for the discogs api.

Look in example-scrapes to understand how the scraping works. The pages you are most interested in are named page.html and domain
